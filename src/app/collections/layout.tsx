import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Collections | SHRINGARIKA',
  description: 'Explore our curated collections of bridal lehengas, designer sarees, festive wear, western fusion, and ready-to-wear — each piece a masterpiece of Indian craftsmanship.',
};

export default function CollectionsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
